import React from "react";

import {
  View,
} from "react-native";
import { style } from "./style";

type Props = {
    caption: string,
    color: string
};

export function Flag({ ...rest }: Props) {
  return (
    
  );
}
